{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local-only capabilities. The remote SahamLens web app is deliberately denied Tauri IPC access.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
